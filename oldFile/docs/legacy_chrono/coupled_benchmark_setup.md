# Coupled Benchmark Automation Setup

以前は Appendix（削除済み）で運用手順を管理していましたが、現在は本リポジトリで Coupled ベンチマークを自動化していません。必要であれば各チーム環境で `tools/run_coupled_benchmark.py` / `tools/build_coupled_benchmark_site.py` を直接呼び出してください。本ファイルは将来的に完全削除予定です。
