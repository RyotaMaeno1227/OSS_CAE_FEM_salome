# tools/exporters/golden_examples/local_patch

This directory holds the local-patch golden example exporter moved from
`scripts/`.

- `export_local_patch_generic_golden_example.sh`

Policy:

- local-patch golden export is support tooling
- export logic is unchanged by relocation
- old `scripts/export_local_patch_generic_golden_example.sh` remains a
  compatibility wrapper
- solver core remains native implementation
