# tools/checkers/routes/contact_circle

This directory holds live contact-circle route checkers moved from `scripts/`
during Phase 1F-a.

Policy:

- these entrypoints are support tooling
- checker logic is unchanged by relocation
- old `scripts/check_contact_circle_*.sh` paths remain compatibility wrappers
- solver core remains native implementation
