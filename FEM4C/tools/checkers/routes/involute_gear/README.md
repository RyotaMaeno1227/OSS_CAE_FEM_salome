# tools/checkers/routes/involute_gear

This directory holds live involute-gear route checkers moved from `scripts/`
during Phase 1F-a.

Policy:

- these entrypoints are support tooling
- checker logic is unchanged by relocation
- old `scripts/check_involute_gear_*.sh` paths remain compatibility wrappers
- solver core remains native implementation
