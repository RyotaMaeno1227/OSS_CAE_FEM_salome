# tools/checkers/routes/gear_pin_proxy

This directory holds live gear-pin proxy route checkers moved from `scripts/`
during Phase 1F-a.

Policy:

- these entrypoints are support tooling
- checker logic is unchanged by relocation
- old `scripts/check_gear_pin_proxy_*.sh` paths remain compatibility wrappers
- solver core remains native implementation
