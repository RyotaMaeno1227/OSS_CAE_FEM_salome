# tools/checkers/routes/mixed_macro_local

This directory holds route-specific live mixed macro-local checkers moved from
`scripts/` during Phase 1F-b.

Policy:

- these entrypoints are support tooling
- checker logic is unchanged by relocation
- old `scripts/check_mixed_macro_local_*.sh` paths remain compatibility wrappers
- canonical mixed bounded route remains closeout済み
- first-class feedback remains `gamma_n`
- solver core remains native implementation
